/** @module cloud-api — Domain. SCAFFOLD. */
export class BrollAnalyzer {
  /** PLACEHOLDER — Extrai keywords de um trecho de transcrição para sugerir b-roll */
  static extractKeywords(_text: string): string[] {
    throw new Error('PLACEHOLDER — BrollAnalyzer requer NLP real');
  }
}
